# Required: deployer private key for Hardhat
PRIVATE_KEY=0xabc...your_private_key

# Required: RPC endpoint to JSC Kaigan.
# Accepts either a full URL (https://rpc.kaigan.jsc.dev/rpc?token=YOUR_TOKEN)
# or just a token value (YOUR_TOKEN). If a token is provided, the URL becomes:
#   https://rpc.kaigan.jsc.dev/rpc?token=YOUR_TOKEN
JSC_RPC_KEY=YOUR_JSC_RPC_ENDPOINT_OR_TOKEN

# Required: Mizuhiki Verified SBT contract address (Kaigan only)
SBT_CONTRACT_ADDRESS=0x606F72657e72cd1218444C69eF9D366c62C54978

# Optional: Owner for deployments (defaults to deployer)
OWNER_ADDRESS=0xOwnerAddress

# ERC20 token used by the PrivacyPool (defaults to MJPY on Kaigan)
ERC20_TOKEN_ADDRESS=0x115e91ef61ae86FbECa4b5637FD79C806c331632

# Verifier contract address (deploy mock for demo or set real verifier)
VERIFIER_ADDRESS=0xVerifierAddress

# PrivacyPool parameters
DENOMS_UNITS=1,10,100
ERC20_DECIMALS=18
INCLUSION_DELAY_BLOCKS=20

# Existing pool address (for root publishing)
PRIVACY_POOL_ADDRESS=0xPoolAddress
